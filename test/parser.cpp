#include <iostream>
#include <unordered_map>
#include "program_options.hpp"

auto main() -> int {
  /* Just a test source file going to be updated frequently. */
  std::unordered_map<tappo::Option, int> um;
  return 0;
}